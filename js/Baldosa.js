/* ************************************************************************************************************ */

import { CASILLAS } from "./Constantes.js";

/* ************************************************************************************************************ */

export default class Baldosa {
	#x;
	#y;
	#tipo;
	
	/* ******************************************************************************************************** */
	
	constructor ( p_pos_x, p_pos_y, p_tipo ) {
		this.#x = p_pos_x;
		this.#y = p_pos_y;
		this.#tipo = p_tipo;
	}
	
	/* ******************************************************************************************************** */
	
	esObstaculo () {
		let obstaculo = false;
		
		if ( this.#tipo === CASILLAS.obstaculo ) {
			obstaculo = true;
		}
		
		return obstaculo;
	}
	
	/* ******************************************************************************************************** */
	
	estoyDentro ( p_pos_x, p_pos_y ) {
		let dentro = false;
		
		if ( this.#x <= p_pos_x && ( this.#x + 1 ) > p_pos_x && this.#y <= p_pos_y && ( this.#y + 1 ) > p_pos_y  ) {
			dentro = true;
		}
		
		return dentro;
	}
	
	/* ******************************************************************************************************** */
	
	obtenerRectangulo () {
		const rectangulo = {
			x: null,
			y: null,
			ancho: null,
			alto: null
		};
	
		rectangulo.x = this.#x;
		rectangulo.y = this.#y;
	
		rectangulo.ancho = 1;
		rectangulo.alto  = 1;
	
		return rectangulo;
	}
	
	/* ******************************************************************************************************** */
}

/* ************************************************************************************************************ */
